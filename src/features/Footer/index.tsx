import { MdPanoramaFishEye } from "react-icons/md";

const Footer = () => {
    const onClick = () => {
        // setShowPassword((prev) => !prev);
    };

    return <div>
    </div>
}
export default Footer;